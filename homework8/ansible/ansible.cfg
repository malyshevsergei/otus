[defaults]
inventory       = inventory/hosts.yml
roles_path      = roles/
remote_user     = ubuntu
host_key_checking = False

[privilege_escalation]
become      = True
become_user = root

[ssh_connection]
pipelining  = True
timeout     = 30
