<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Backend {{ inventory_hostname }}</title>
  <style>
    body { font-family: monospace; padding: 2rem; background: #f0f0f0; }
    h1 { color: #333; }
    .info { background: #fff; padding: 1rem; border-radius: 4px; }
  </style>
</head>
<body>
  <h1>Backend Node</h1>
  <div class="info">
    <p><strong>Hostname:</strong> {{ inventory_hostname }}</p>
    <p><strong>Private IP:</strong> {{ ansible_default_ipv4.address }}</p>
  </div>
</body>
</html>
