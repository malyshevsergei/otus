---
# Nginx role defaults

nginx_user: nginx
nginx_worker_processes: auto
nginx_worker_connections: 1024

# Upstream backend servers
backend_servers: "{{ groups['backend'] }}"

# Static files location
static_root: "/var/www/static"
media_root: "/var/www/media"
